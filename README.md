# Arun Govindgari — Portfolio

**Live Site:** https://arungovindgari.github.io  
**Stack:** HTML5 · CSS3 · Bootstrap 5 · Vanilla JS · Python FastAPI  


```bash
# Install Git: https://git-scm.com/downloads
git init
git add .
git commit -m "Initial portfolio deployment"
git branch -M main
git remote add origin https://github.com/arungovindgari/arungovindgari.github.io.git
git push -u origin main
```

---

## 📁 File Structure

```
portfolio/
├── index.html                  ← Main portfolio page
├── assets/
│   ├── css/
│   │   └── style.css           ← All styles (responsive, dark/light theme)
│   ├── js/
│   │   └── app.js              ← All interactions (no jQuery, secure)
│   └── Arun_Govindgari_Resume.pdf  ← Your resume (add this file!)
├── data/
│   └── portfolio.json          ← All your content (edit this to update)
├── backend/
│   ├── main.py                 ← Python FastAPI contact form API
│   ├── requirements.txt
│   └── .env.example            ← Copy to .env, add credentials
├── .github/
│   └── workflows/
│       └── deploy.yml          ← Auto-deploy to GitHub Pages
├── .gitignore
└── README.md
```
